# 추상화
## '='의 의미
등호의 의미가 아닌 값을 지정하는 지정연산자이다.
## 함수 실행순서
```
def hello():
    print("Hello!")
    print("Welcome to Codeit!")

print("함수 호출 전")
hello() #함수 실행
print("함수 호출 후")

def square(x):
    return x*x

print("함수 호출 전")
print(square(3)+square(4)) #함수 실행
print("함수 호출 후")
```

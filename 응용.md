# 응용
## 모듈
모듈을 사용하는 이유:한 파일에 함수와 실행부분을 넣게 되면 코드가 너무 길어질 수 있음
> 따라서 실행부분은 따로 파일을 만들어서 함수파일을 따로 호출함

호출방법
* 새 파일 생성 후 호출
ex) 함수 파일이름:calculator
    add, sub, mul, div 함수가 존재
```
import calculator #calculator파일 호출

print(calculator.add(2,5))
print(calculator.mul(3,5))
```
* 호출 시 파일이름을 임의로 지정할 수 있음
```
import calculator as calc #호출시 calc로 작성하여 호출

print(calc.add(2,5))
print(calc.mul(3,5))
```
* 사용할 함수만 호출하고 싶을 때
```
from calculator import add, mul

print(add(2,5))
print(mul(3,4))
```
## 스탠다드 라이브러리
* math 모듈
```
import math

print(math.log10(100))
print(math.cos(0))
print(math.pi)
```
* random 모듈
```
import random

print(random.random())
```
* 운영체제
```
import os

print(os.getlogin()) #로그인된 계정
print(os.getcwd())  #파일 경로
```
### datetime모둘
```
import datetime
#datetime값 생성
pi_day=datetime.datetime(2022,7,6)
print(pi_day)
print(type(pi_day))

#오늘 날짜
pi_day=datetime.datetime(2022,7,6,11,51,45)
print(pi_day)
print(type(pi_day))

#timedelta
today=datetime.datetime.now()
pi_day=datetime.datetime(2022,7,6,11,51,45)
print(today-pi_day)
print(type(today-pi_day))

today=datetime.datetime.now()
my_timedelta=datetime.timedelta(days=5,hours=3,minutes=10,seconds=50)
print(today)
print(today+my_timedelta)

#datetime해부하기
today=datetime.datetime.now()

print(today)
print(today.year)
print(today.month)
print(today.day)
print(today.hour)
print(today.minute)
print(today.second)
print(today.microsecond)

#datetime 포맷팅
today=datetime.datetime.now()

print(today)
print(today.strftime("%A, %B %dth %Y"))
```
